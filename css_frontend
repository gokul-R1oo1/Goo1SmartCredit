/* CSS code to style the navigation bar */
.navbar {
    background-color: #222;
    color: #fff;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px;
}

/* CSS code to style the navigation button */
.nav-button {
    background-color: #fff;
    color: #222;
    padding: 10px 20px;
    border: none;
    cursor: pointer;
}

/* CSS code to style the loan application form */
.loan-form {
    margin: 50px auto;
    width: 500px;
    background-color: #f5f5f5;
    padding: 20px;
}

/* CSS code to style the form fields */
.loan-form input {
    display: block;
    width: 100%;
    margin-bottom: 10px;
    padding: 10px;
    border: none;
    border-bottom: 2px solid #ccc;
}

/* CSS code to style the submit button */
.loan-form button {
    background-color: #222;
    color: #fff;
    padding: 10px 20px;
    border: none;
    cursor: pointer;
}
